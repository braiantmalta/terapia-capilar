import { styled } from '@mui/system';

export const AboutSessionContent = styled('div')({
  display: 'flex',
  height: 'content-fit',
  flexDirection: 'column',
  alignItems: 'center',
});

export const TextBox = styled('div')({
  width: '1000px',
  marginTop: '17px',
});

export const TeachersBox = styled('div')({
  display: 'flex',
  flexDirection: 'row',
  gap: '220px',
  margin: '30px',
});

import { TextField } from '@mui/material';
import { Wrapper } from './styles';

export const Home = () => {
  return (
    <Wrapper>
      <TextField label="label" name="name" />
    </Wrapper>
  );
};
